import { Template } from "aws-cdk-lib/assertions"
import { App, Stack } from "aws-cdk-lib"
import { Thumbnail, ThumbnailStackProps } from "../src/index"

// describe("Test upload image to s3", () => {
//   const app = new App()
//   const stack = new Stack(app, "testing-cc-thumbnail")
//   new Thumbnail(stack, "TestThumbnail", {} as ThumbnailStackProps)

//   test("Expect can find image in s3", () => {})
//   test("Expect can find resized image in s3", () => {})
// })
